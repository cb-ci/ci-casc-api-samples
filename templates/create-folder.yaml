removeStrategy:
  rbac: SYNC
  items: NONE
items:
- kind: folder
  name: ${FOLDER_NAME}
  description: ${FOLDER_NAME}
  displayName: ${FOLDER_NAME}
  properties:
  - envVars: {
      }
  - itemRestrictions:
      filter: false